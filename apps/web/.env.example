# -- Database --------------------------------------------------------
DATABASE_URL=postgresql://studylab:studylab_dev_2024@localhost:5433/ccna_studylab

# -- Auth.js ---------------------------------------------------------
# Generate with: openssl rand -base64 32
AUTH_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_base64_32

# -- AI Tutor (optional) ---------------------------------------------
# NOTE: Uses TUTOR_ANTHROPIC_KEY (not ANTHROPIC_API_KEY) to avoid
# conflicts with Claude Code's environment variables.
# TUTOR_ANTHROPIC_KEY=sk-ant-api03-...

# -- Lab Engine (optional) -------------------------------------------
# URL for the Docker lab engine service (used server-side only).
# Do NOT use NEXT_PUBLIC_ prefix — this is a server-only variable.
# LAB_ENGINE_URL=http://localhost:8100/api/v1/grade
# LAB_ENGINE_API_KEY=

# -- Security --------------------------------------------------------
# WARNING: Never set SKIP_AUTH=true in production. It disables all
# authentication checks and is intended only for local E2E testing.
